# MOVED TO SUPABASE pg_cron (migration: crons_via_pg_cron) on 2026-06-25.
# Schedule disabled to avoid double-execution with the Supabase scheduler.
# The job now runs as public.cron_* inside Postgres. Manual trigger kept for reference.
name: Hourly FA Market (moved to Supabase pg_cron)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This cron moved to Supabase pg_cron (public.cron_*). Nothing to do here."
